# Apple 客户端

当前版本不支持 iOS、iPadOS、tvOS 或 macOS 官方 Tailscale 客户端。

这些客户端依赖浏览器/OIDC 等官方认证流程，而 Headscale-Admin-AE 只接受 ScaleTail
账户密码 Noise 认证。在对应平台的 ScaleTail 客户端正式发布并完成同协议测试前，
不要启用 OIDC、手工注册或预认证 Key 作为兼容方案。
